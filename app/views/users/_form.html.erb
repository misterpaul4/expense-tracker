<%= simple_form_for @user, class: "w-100" do |f| %>
  <div class="primary-color pt-4 pb-2 px-3 d-flex justify-content-between align-items-center">
    <%= link_to welcome_path do %>
    <%= image_tag ("icons/back.svg") %>
    <% end %>

    <span class="nav-header text-white">SIGNUP</span>

    <%= f.submit 'Next', class: "submit-btn text-light" %>
  </div>
  <%= f.input :username, placeholder: "Username", label: false, input_html: { class: "w-100 px-3 py-3 mt-3 border-0 mid-font" } %>
  <%= f.input :budget, placeholder: "Initial Budget", :input_html => {value: number_with_precision(f.object.budget, precision: 2), class: "w-100 px-3 py-3 mt-2 border-0 mid-font" }, label: false %>
  <div class="w-100 px-3 py-3 mt-2 border-0 mid-font bg-white d-flex justify-content-between align-items-center">
    <div>Currency: </div>
    <%= f.currency_select(:currency, ["USD", "EUR", "GBP"], {}, {class: "select-box text-right overflow-hidden px-3 border-0 bg-white text-success"})%>
  </div>
<% end %>
<span class="text-right d-block pr-3 pt-2">select currency from dropdown</span>
