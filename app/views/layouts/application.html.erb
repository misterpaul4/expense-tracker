<!DOCTYPE html>
<html>
  <head>
    <title>ExpenseTracker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if user_logged_in? %>
        <span>logged in as: <%= current_user.username %> <%= link_to 'Edit', edit_user_path(current_user) %>
        </span>
        <span> <%= link_to "Logout", logout_path, method: :delete %> </span>
        <p>add new expense category: <%= link_to 'New Category', new_expense_path %></p>
        <p><strong>total expenses:</strong> <%= amount(total_expenses(current_user))%></p>
        <%= balance %>
      <% end %>
    </nav>
    <% if notice.present? %>
      <p> <%= notice %> </p>
    <% end %>
    <% if alert.present? %>
      <p> <%= alert %> </p>
    <% end %>
    <%= yield %>
  </body>
</html>
